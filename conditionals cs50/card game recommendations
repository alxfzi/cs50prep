def main():
    # making sure the values inputted are the ones required
    difficulty = input("difficult or casual? ")
    if not (difficulty == "difficult" or difficulty == "casual"):
        print("please enter a valid difficulty")
        return

    players = input("multiplayer or singleplayer? ")
    if not(players == "mutliplayer" or players == "singleplayer"):
        print("please enter a valid number of players")
        return

# return values
    if difficulty == "difficult" and players == "multiplayer":
            recommend("poker")
    elif difficulty == "difficult" and players == "singleplayer":
            recommend("klondlike")
    elif difficulty == "casual" and players == "multiplayer":
            recommend("hearts")
    elif difficulty == "casual" and players == "singleplayer":
            recommend("clock")

def recommend(game):
    print("you might like", game)

main()
